<div class="container">
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
      <%= value %>
    </div>
  <% end %>
  <%= yield %>
</div>

<div class="page-header"><h1>Welcome!</h1></div>

<% if can? :index, Classroom %>
  <%= link_to 'Classrooms', classrooms_path, class: 'btn btn-lg btn-primary' %>
<% end %>